{% set name = "pyfiglet" %}
{% set version = "1.0.3" %}
{% set compress_type = "tar.gz" %}
{% set hash = "bad3b55d2eccb30d4693ccfd94573c2a3477dd75f86a0e5465cea51bdbfe2875" %}
{% set build = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ compress_type }}
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ compress_type }}
  sha256: {{ hash }}

build:
  noarch: python
  entry_points:
    - pyfiglet = pyfiglet:main
  number: {{ build }}
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  imports:
    - pyfiglet
    - pyfiglet.fonts

  commands:
    - pyfiglet --help

about:
  home: https://github.com/pwaller/pyfiglet
  summary: 'Pure-python FIGlet implementation'
  # license_file not in MANIFEST, so including local copy - see https://github.com/pwaller/pyfiglet/pull/52
  license_file: {{ RECIPE_DIR }}/LICENSE
  license: GPL2
  license_family: GPL2
  dev_url: https://github.com/pwaller/pyfiglet

extra:
  recipe-maintainers:
    - pmlandwehr
